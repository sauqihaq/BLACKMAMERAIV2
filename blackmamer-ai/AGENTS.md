# BlackMamer AI — Agent Notes

Read `DESIGN.md` before changing UI.

For UI, copy, accessibility, responsive layout, or code-comment work, apply the anti-slop principles from:
https://github.com/miqdadbadjuber/anti-slop

The application runtime also carries a concise anti-slop contract in `api/chat.js` for website/UI generation.

Do not replace the existing authentication/session/API contracts unless the task explicitly requires it.
